I want to create a web-based version of this OverPrime calculator. 

Please create the following files inside the `docs/overprime/` directory to implement it as a clean, responsive single-page web app:
1. `docs/overprime/index.html` - The structural layout (semantic HTML5, form inputs for the calculator parameters, and a results display area).
2. `docs/overprime/overprime.css` - Modern, clean styling. Use a Forge of Empires-inspired color palette (warm golds, deep grays/browns). Ensure it is mobile-friendly.
3. `docs/overprime/overprime.js` - Port the exact mathematical logic, formulas, and rules from the provided Python file into clean JavaScript. It should listen for input changes or a form submission, run the calculations dynamically, and update the HTML results area without refreshing the page.

Also, please create a basic landing page at `docs/index.html` that serves as a dashboard and includes a link/card pointing to the new OverPrime calculator (`./overprime/index.html`).

Keep the JavaScript modular and well-commented so it matches the clarity of the Python logic.
